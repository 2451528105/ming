---
description: Git 提交信息默认使用简体中文
alwaysApply: true
---

# Git 提交信息（简体中文）

在使用「源代码管理 / Source Control」中的 **生成提交信息**（输入框旁星标/魔法按钮）或为该仓库 **自动生成 Git commit** 时：

- **标题与正文须使用简体中文**，不要默认输出英文。
- **标题**：简短说明做了什么；可用 `模块或范围: 摘要`（范围、包名、标识符可保留英文）。
- **正文**（若有多行）：用完整句子写背景、改动要点与影响；路径、API、协议字段可与中文混排。
- 仅当操作者**明确要求英文** commit 时，才以英文为主撰写。
